{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"StellarDrive Modding API","text":"<p>Welcome to the documentation for the StellarDrive Modding API.</p> <p>The StellarDrive Modding API allows developers to create custom content for StellarDrive through external mods.</p> <p>Currently, the API provides tools for creating custom parts and working with custom assets through Unity AssetBundles.</p>"},{"location":"#features","title":"Features","text":"<p>Current features:</p> <ul> <li>Create custom parts</li> <li>Define custom part properties and building costs</li> <li>Load custom assets from Unity AssetBundles</li> <li>Register custom content through the API</li> </ul> <p>The API is actively being expanded with more modding capabilities.</p>"},{"location":"#getting-started","title":"Getting Started","text":"<p>New to StellarDrive modding?</p> <p>Start by setting up your development environment:</p> <p>Getting Started</p> <p>This section covers:</p> <ul> <li>Installing MelonLoader</li> <li>Setting up a mod project</li> <li>Creating your first mod</li> <li>Building and testing your mod</li> </ul>"},{"location":"#documentation","title":"Documentation","text":""},{"location":"#parts","title":"Parts","text":"<p>Learn how to create and register custom parts:</p> <ul> <li>Part creation</li> <li>Part definitions</li> <li>Part properties</li> <li>Building costs</li> </ul> <p>Parts</p>"},{"location":"#assets","title":"Assets","text":"<p>Learn how to load and use custom assets:</p> <ul> <li>Unity AssetBundles</li> <li>AssetLoader</li> <li>Supported asset types</li> </ul> <p>Assets</p>"},{"location":"assets/","title":"Assets","text":"<p>The StellarModdingAPI provides tools for loading custom Unity assets from AssetBundles included with a mod.</p> <p>Assets are commonly used when creating custom parts, such as:</p> <ul> <li>Part prefabs</li> <li>Part thumbnails</li> <li>Custom models and textures</li> <li>Other Unity objects supported by AssetBundles</li> </ul>"},{"location":"assets/#asset-workflow","title":"Asset Workflow","text":"<p>Using custom assets usually follows these steps:</p> <ol> <li>Create an AssetBundle in Unity</li> <li>Embed the AssetBundle in your mod assembly</li> <li>Create an <code>AssetLoader</code></li> <li>Retrieve assets by name using <code>GetAsset&lt;T&gt;()</code></li> </ol> <p>Optionally, you can use <code>AssetUtilities</code> to generate the asset name list automatically instead of writing it by hand.</p>"},{"location":"assets/#related-guides","title":"Related Guides","text":"<p>For an example of using assets in a custom part, see:</p> <p>Creating a Part</p>"},{"location":"assets/asset-loader-reference/","title":"AssetLoader","text":"<p><code>AssetLoader</code> loads assets from AssetBundles embedded in your mod assembly.</p> <p>Namespace:</p> <pre><code>using StellarModdingAPI.Assets;\n</code></pre>"},{"location":"assets/asset-loader-reference/#creating-an-assetloader","title":"Creating an AssetLoader","text":"<p>Example:</p> <pre><code>Loader = new AssetLoader(\n    MelonAssembly.Assembly,\n    LoggerInstance,\n    new[]\n    {\n        \"PottedPlant\", \"PottedPlantThumbnail\"\n    }\n);\n</code></pre> <p>The names provided to <code>AssetLoader</code> are the asset names that your mod wants to load.</p> <p>In this example:</p> <ul> <li><code>PottedPlant</code> is a GameObject prefab.</li> <li><code>PottedPlantThumbnail</code> is a Texture2D image.</li> </ul> <p>A logger isn't required. If you don't provide one, <code>AssetLoader</code> simply won't log anything, but it will still load assets normally.</p> <p>Note</p> <p><code>AssetLoader</code> should be created after <code>OnLateInitializeMelon</code>. Creating it earlier may cause an error, since Unity's asset systems may not be fully initialized yet.</p>"},{"location":"assets/asset-loader-reference/#loading-assets","title":"Loading Assets","text":"<p>After creating an <code>AssetLoader</code>, assets can be retrieved using:</p> <pre><code>Loader.GetAsset&lt;T&gt;(\"AssetName\")\n</code></pre> <p>Example:</p> <pre><code>GameObject prefab = Loader.GetAsset&lt;GameObject&gt;(\"PottedPlant\");\n\nTexture2D thumbnail = Loader.GetAsset&lt;Texture2D&gt;(\"PottedPlantThumbnail\");\n</code></pre> <p>The generic type should match the Unity asset type being loaded.</p>"},{"location":"assets/asset-loader-reference/#asset-requirements","title":"Asset Requirements","text":"<p>Assets must:</p> <ul> <li>Be included in an AssetBundle</li> <li>Have a unique asset name</li> <li>Be included when creating the AssetLoader</li> </ul> <p>If an asset cannot be found, <code>AssetLoader</code> will log an error and return null.</p>"},{"location":"assets/asset-loader-reference/#supported-asset-types","title":"Supported Asset Types","text":"<p><code>AssetLoader</code> can load any Unity Object stored in an AssetBundle.</p> <p>Examples:</p> Type Usage GameObject Part prefabs and models Texture2D Build menu thumbnails and textures Material Custom materials AudioClip Custom audio <p>Note</p> <p>As said before, those are examples, not every supported asset type!</p>"},{"location":"assets/asset-utilities-reference/","title":"AssetUtilities","text":"<p><code>AssetUtilities</code> automatically extracts asset names from a class or struct containing asset keys, so you don't have to maintain the name list by hand when calling <code>AssetLoader</code>.</p> <p>Namespace:</p> <pre><code>using StellarModdingAPI.Assets;\n</code></pre>"},{"location":"assets/asset-utilities-reference/#extracting-asset-keys","title":"Extracting Asset Keys","text":"<p>Example:</p> <pre><code>[AssetKeyCollection]\npublic static class AssetKeys\n{\n    public const string PottedPlant = \"PottedPlant\";\n    public const string PottedPlantThumbnail = \"PottedPlantThumbnail\";\n}\n</code></pre> <p>The keys can then be extracted using:</p> <pre><code>AssetUtilities.ExtractAssetKeysFrom(typeof(AssetKeys));\n</code></pre> <p>This returns:</p> <pre><code>[\n    \"PottedPlant\",\n    \"PottedPlantThumbnail\"\n]\n</code></pre>"},{"location":"assets/asset-utilities-reference/#asset-key-attributes","title":"Asset Key Attributes","text":"<p>Individual fields can also be marked using <code>[AssetKey]</code>, instead of marking the whole class with <code>[AssetKeyCollection]</code>:</p> <pre><code>public static class AssetKeys\n{\n    [AssetKey]\n    public const string PottedPlant = \"PottedPlant\";\n}\n</code></pre> <p>Only constant string fields can be used as asset keys.</p> <p>Warning</p> <p>When a class is marked with <code>[AssetKeyCollection]</code>, every field on it is treated as an asset key, not just the ones you intend. If any field on that class isn't a <code>const string</code>, <code>ExtractAssetKeysFrom</code> will throw an exception.</p> <p>Keep <code>[AssetKeyCollection]</code> classes dedicated to asset keys only, or use <code>[AssetKey]</code> on individual fields in a mixed-purpose class instead.</p>"},{"location":"assets/creating-asset-bundles/","title":"Creating AssetBundles","text":"<p>Before assets can be loaded by <code>AssetLoader</code>, they need to be packaged into an AssetBundle inside the Unity Editor.</p>"},{"location":"assets/creating-asset-bundles/#1-assign-an-assetbundle-name","title":"1. Assign an AssetBundle Name","text":"<ol> <li>Select the asset in the Project window (e.g. a prefab, texture, or material).</li> <li>In the Inspector, open the AssetBundle dropdown at the bottom.</li> <li>Create a new bundle name (e.g. <code>pottedplant</code>) or select an existing one.</li> </ol> <p>Tip</p> <p>Assets that should be loaded together can share the same bundle name. <code>AssetLoader</code> reads every embedded AssetBundle it finds, so you don't need a separate bundle per asset. Feel free to manage bundles however you want.</p>"},{"location":"assets/creating-asset-bundles/#2-set-the-assets-name","title":"2. Set the Asset's Name","text":"<p><code>AssetLoader</code> matches assets by their <code>Object.name</code>, which is normally just the file name of the asset in the Project window. Rename the asset file to match the key you plan to use, for example:</p> <ul> <li>PottedPlant.prefab</li> <li>PottedPlantThumbnail.png</li> </ul>"},{"location":"assets/creating-asset-bundles/#3-build-the-assetbundle","title":"3. Build the AssetBundle","text":"<p>Add a build script under an <code>Editor</code> folder in your Unity project:</p> <pre><code>using UnityEditor;\nusing UnityEngine;\nusing System.IO;\n\npublic class BuildAssetBundles\n{\n    [MenuItem(\"Bundles/Build AssetBundles\")]\n    public static void BuildAllAssetBundles()\n    {\n        string outputPath = \"Assets/AssetBundles\";\n\n        if (!Directory.Exists(outputPath))\n            Directory.CreateDirectory(outputPath);\n\n        BuildPipeline.BuildAssetBundles(\n            outputPath,\n            BuildAssetBundleOptions.None,\n            BuildTarget.StandaloneWindows64\n        );\n\n        Debug.Log(\"AssetBundles built successfully!\");\n    }\n}\n</code></pre> <p>Running this from Bundles \u2192 Build AssetBundles produces a file named after your bundle (e.g. <code>pottedplant</code>) with no file extension. This is the file you'll embed into your mod assembly.</p> <p>Warning</p> <p>Build the AssetBundle against the same Unity version (and ideally the same target platform) as the game you're modding, otherwise Unity may fail to load it at runtime.</p>"},{"location":"assets/creating-asset-bundles/#next-step","title":"Next Step","text":"<p>Continue to Embedding Assets in Your Mod to include the built AssetBundle in your mod's assembly.</p>"},{"location":"assets/embedding-assets/","title":"Embedding Assets in Your Mod","text":"<p><code>AssetLoader</code> expects each AssetBundle to be included in your mod's assembly as an Embedded Resource. This packages the bundle directly into your compiled <code>.dll</code>, so no extra files need to be shipped alongside it.</p> <p>Info</p> <p>This guide assumes you're using the StellarDrive-ModTemplate, which is an SDK-style project (<code>&lt;Project Sdk=\"Microsoft.NET.Sdk\"&gt;</code>).</p>"},{"location":"assets/embedding-assets/#1-add-the-assetbundle-file-to-your-project","title":"1. Add the AssetBundle File to Your Project","text":"<p>Copy the AssetBundle file you built in Unity (e.g. <code>pottedplant</code>) into your mod project's folder, for example:</p> <pre><code>ModTemplate/\n\u251c\u2500\u2500 Assets/\n\u2502   \u2514\u2500\u2500 pottedplant\n\u251c\u2500\u2500 Main.cs\n\u2514\u2500\u2500 ModTemplate.csproj\n</code></pre>"},{"location":"assets/embedding-assets/#2-embed-it-via-the-csproj","title":"2. Embed It via the .csproj","text":"<p>Open <code>ModTemplate.csproj</code> and add an <code>EmbeddedResource</code> entry pointing at the file:</p> <pre><code>&lt;ItemGroup&gt;\n  &lt;EmbeddedResource Include=\"Assets\\pottedplant\" /&gt;\n&lt;/ItemGroup&gt;\n</code></pre> <p>The full file will look something like this:</p> <pre><code>&lt;Project Sdk=\"Microsoft.NET.Sdk\"&gt;\n\n  &lt;PropertyGroup&gt;\n    &lt;TargetFramework&gt;netstandard2.1&lt;/TargetFramework&gt;\n    &lt;LangVersion&gt;latest&lt;/LangVersion&gt;\n    &lt;Nullable&gt;enable&lt;/Nullable&gt;\n    &lt;GenerateAssemblyInfo&gt;false&lt;/GenerateAssemblyInfo&gt;\n    &lt;OutputPath&gt;$(SolutionDir)Output\\&lt;/OutputPath&gt;\n  &lt;/PropertyGroup&gt;\n\n  &lt;ItemGroup&gt;\n    &lt;None Remove=\"Assets\\pottedplant\" /&gt;\n  &lt;/ItemGroup&gt;\n\n  &lt;ItemGroup&gt;\n    &lt;EmbeddedResource Include=\"Assets\\pottedplant\" /&gt;\n  &lt;/ItemGroup&gt;\n\n  &lt;ItemGroup&gt;\n    &lt;Reference Include=\"ReferencedAssemblies\\MelonLoader.dll\" /&gt;\n    &lt;Reference Include=\"ReferencedAssemblies\\UnityEngine.CoreModule.dll\" /&gt;\n    &lt;Reference Include=\"ReferencedAssemblies\\UnityEngine.dll\" /&gt;\n  &lt;/ItemGroup&gt;\n\n&lt;/Project&gt;\n</code></pre>"},{"location":"assets/embedding-assets/#next-step","title":"Next Step","text":"<p>Continue to the AssetLoader Reference to load the assets from the embedded bundle.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This section will guide you through creating your first StellarDrive mod using the StellarModdingAPI.</p> <p>Before creating mods, make sure you have the required tools installed:</p> <ul> <li>StellarDrive installed</li> <li>MelonLoader installed</li> <li>Visual Studio 2022</li> <li>The StellarDrive Mod Template</li> </ul> <p>If you have not installed MelonLoader yet, start here:</p> <p>Installing MelonLoader</p> <p>Once your environment is ready, continue with:</p> <p>Creating Your First Mod</p>"},{"location":"getting-started/creating-your-first-mod/","title":"Creating Your First Mod","text":"<p>In this guide, you will create a simple StellarDrive mod using the StellarDrive Mod Template.</p> <p>The template contains a basic mod that writes a message to the MelonLoaderconsole when the game scene is loaded. We will configure it, build it, and verify that it is working.</p>"},{"location":"getting-started/creating-your-first-mod/#setting-up-the-template","title":"Setting Up the Template","text":"<p>Download the StellarDrive Mod Template:</p> <p>StellarDrive-ModTemplate</p> <p>After downloading the template:</p> <ol> <li>Open the solution in Visual Studio 2022.</li> <li>Rename the solution and project to your mod's name.</li> <li>Open <code>Main.cs</code>.</li> <li>Rename the namespace to match your mod name.</li> <li>Rename the main mod class to match your mod name.</li> <li>Fill in the <code>BuildInfo</code> information.</li> </ol> <p>Your <code>Main.cs</code> should look similar to:</p> <pre><code>using MelonLoader;\n\nnamespace MyFirstMod\n{\n    public static class BuildInfo\n    {\n        public const string Name = \"My First Mod\";\n        public const string Description = \"My first StellarDrive mod\";\n        public const string Author = \"Your Name\";\n        public const string Company = null;\n        public const string Version = \"1.0.0\";\n        public const string DownloadLink = null;\n    }\n\n    public class MyFirstMod : MelonMod\n    {\n        public override void OnSceneWasLoaded(int buildIndex, string sceneName)\n        {\n            // This checks if the game scene loaded\n            if (buildIndex != 1) return;\n\n            MelonLogger.Msg(\"This mod seems to work\");\n        }\n\n        public override void OnSceneWasUnloaded(int buildIndex, string sceneName)\n        {\n            // This checks if the game scene unloaded\n            if (buildIndex != 1) return;\n        }\n    }\n}\n</code></pre>"},{"location":"getting-started/creating-your-first-mod/#updating-assemblyinfo","title":"Updating AssemblyInfo","text":"<p>Open:</p> <pre><code>Properties/AssemblyInfo.cs\n</code></pre> <p>Replace the old template class names with your new mod class name.</p> <p>For example:</p> <pre><code>ModTemplate.BuildInfo\n</code></pre> <p>becomes:</p> <pre><code>MyFirstMod.BuildInfo\n</code></pre> <p>and:</p> <pre><code>ModTemplate.ModTemplate\n</code></pre> <p>becomes:</p> <pre><code>MyFirstMod.MyFirstMod\n</code></pre> <p>This allows MelonLoader to correctly identify your mod.</p>"},{"location":"getting-started/creating-your-first-mod/#understanding-the-mod-class","title":"Understanding the Mod Class","text":"<p>Every StellarDrive mod inherits from <code>MelonMod</code>.</p> <pre><code>public class MyFirstMod : MelonMod\n{\n}\n</code></pre> <p>MelonLoader automatically creates an instance of this class when the game starts.</p>"},{"location":"getting-started/creating-your-first-mod/#responding-to-game-events","title":"Responding to Game Events","text":"<p>The template overrides two methods:</p> <pre><code>public override void OnSceneWasLoaded(int buildIndex, string sceneName)\n{\n    if (buildIndex != 1) return;\n\n    MelonLogger.Msg(\"This mod seems to work\");\n}\n\npublic override void OnSceneWasUnloaded(int buildIndex, string sceneName)\n{\n    if (buildIndex != 1) return;\n}\n</code></pre> <p><code>OnSceneWasLoaded</code> is called whenever a scene is loaded.</p> <p>The example checks that the loaded scene is the main game scene before running any code.</p> <pre><code>if (buildIndex != 1) return;\n</code></pre> <p>This prevents your mod from running in other scenes, like the main menu.</p>"},{"location":"getting-started/creating-your-first-mod/#testing-the-mod","title":"Testing the Mod","text":"<p>Build the project.</p> <p>Copy the generated DLL into the <code>Mods</code> folder.</p> <p>Launch StellarDrive.</p> <p>If everything is working correctly, the MelonLoader console should display:</p> <pre><code>This mod seems to work\n</code></pre> <p>Congratulations! You have successfully created and loaded your first StellarDrive mod.</p> <p>In the next section, you'll use the StellarModdingAPI to create your first custom part.</p> <p>Creating a Part</p>"},{"location":"getting-started/installing-melonloader/","title":"Installing MelonLoader","text":"<p>MelonLoader is the mod loader used by StellarDrive. Whether you want to create mods or simply use them, you'll need to install MelonLoader first.</p>"},{"location":"getting-started/installing-melonloader/#downloading-melonloader","title":"Downloading MelonLoader","text":"<p>Download the MelonLoader installer: MelonLoader</p> <p></p>"},{"location":"getting-started/installing-melonloader/#installing","title":"Installing","text":"<ol> <li> <p>Run the installer. It should look similar to the screenshot below (the list of installed games may differ).</p> <p></p> <p>Note</p> <p>If you own StellarDrive on itch.io, it won't appear automatically in the installer. Click Add Game Manually in the bottom-right corner and select your <code>StellarDrive.exe</code>.</p> <p></p> </li> <li> <p>Press the Install button.</p> <p></p> </li> <li> <p>Verify the installation.</p> <p>After the installation is complete, your StellarDrive folder should contain the following directories:</p> <p></p> <p>Tip</p> <p>If the <code>Mods</code> or <code>Plugins</code> folder is missing, you can create it manually by creating a folder with the same name.</p> </li> </ol>"},{"location":"getting-started/installing-melonloader/#next-step","title":"Next Step","text":"<p>MelonLoader is now installed. Continue by creating your first StellarDrive mod:</p> <p>Creating Your First Mod</p>"},{"location":"parts/","title":"Parts","text":"<p>The StellarModdingAPI allows mods to create custom parts for StellarDrive.</p> <p>Parts are registered through the API and added to the game's build menu, allowing players to use custom content alongside the game's existing parts.</p>"},{"location":"parts/#creating-parts","title":"Creating Parts","text":"<p>A custom part is created using a <code>PartDefinition</code>.</p> <p>A <code>PartDefinition</code> contains the information needed to create a part, including:</p> <ul> <li>Name and description</li> <li>Model and thumbnail assets</li> <li>Size and mass</li> <li>Snapping behavior</li> <li>Building costs</li> </ul> <p>Once a part is registered, the API converts the definition into the game's internal part format and adds it to the build menu.</p>"},{"location":"parts/#part-workflow","title":"Part Workflow","text":"<p>Creating a custom part usually follows these steps:</p> <ol> <li>Load your custom assets</li> <li>Create a <code>PartDefinition</code></li> <li>Register the part</li> <li>Build the part registry</li> <li>Test the part in-game</li> </ol>"},{"location":"parts/#guides","title":"Guides","text":"<p>Start creating your first custom part:</p> <p>Creating a Part</p>"},{"location":"parts/creating-a-part/","title":"Creating a Part","text":"<p>This guide will walk you through creating a custom part and adding it to the StellarDrive build menu.</p> <p>A custom part requires:</p> <ul> <li>A model prefab</li> <li>A thumbnail</li> <li>Basic properties</li> <li>Optional building costs</li> </ul> <p>Parts are created using a <code>PartDefinition</code>, registered through the API, and then added to the game's build menu.</p>"},{"location":"parts/creating-a-part/#loading-assets","title":"Loading Assets","text":"<p>Before creating a part, you need to load the assets used by the part.</p> <p>The StellarModdingAPI provides <code>AssetLoader</code> for loading assets from Unity AssetBundles.</p> <p>Example:</p> <pre><code>Loader = new AssetLoader(\n    MelonAssembly.Assembly,\n    LoggerInstance,\n    new[]\n    {\n        \"PottedPlant\", \"PottedPlantThumbnail\"\n    }\n);\n</code></pre> <p>The asset names passed to <code>AssetLoader</code> are the names of the assets that your mod will use later.</p> <p>In this example:</p> <ul> <li><code>PottedPlant</code> is the part's model prefab.</li> <li><code>PottedPlantThumbnail</code> is the image displayed in the build menu.</li> </ul>"},{"location":"parts/creating-a-part/#initializing-the-api","title":"Initializing the API","text":"<p>Before registering parts, initialize the StellarModdingAPI:</p> <pre><code>StellarModdingAPI.Plugin.Initialize();\n</code></pre> <p>This prepares the internal registries required for custom content.</p>"},{"location":"parts/creating-a-part/#creating-a-part_1","title":"Creating a Part","text":"<p>Parts are created using <code>PartDefinition</code>.</p> <p>A <code>PartDefinition</code> contains all the information required to create a part.</p> <p>Example:</p> <pre><code>PartDefinition PottedPlantDefinition = new()\n{\n    Name = \"Potted Plant\",\n    Description = \"Potted plant!!!!!!!\",\n\n    Prefab = Loader.GetAsset&lt;GameObject&gt;(\"PottedPlant\"),\n    Thumbnail = Loader.GetAsset&lt;Texture2D&gt;(\"PottedPlantThumbnail\"),\n\n    Size = Vector3.one,\n    Mass = 1f,\n\n    Snapping = SnappingStyle.PreciseOnAny\n};\n</code></pre> <p>The <code>PartDefinition</code> contains the information required to create the part, including its name, assets, size, mass, snapping behavior, and optional building costs.</p> <p>For a complete list of available properties, see:</p> <p>PartDefinition Reference</p>"},{"location":"parts/creating-a-part/#registering-parts","title":"Registering Parts","text":"<p>Parts are registered using:</p> <pre><code>StellarModdingAPI.Plugin.RegisterPart(PottedPlantDefinition);\n</code></pre> <p>Registered parts are stored as <code>PartDefinition</code> objects internally. When <code>Build()</code> is called, these definitions are converted into game-compatible parts and added to StellarDrive.</p>"},{"location":"parts/creating-a-part/#building-the-part-registry","title":"Building the Part Registry","text":"<p>After registering all your parts, build the registry:</p> <pre><code>StellarModdingAPI.Plugin.Build();\n</code></pre> <p>This converts the registered part definitions into game-compatible parts and adds them to StellarDrive's build menu.</p>"},{"location":"parts/creating-a-part/#cleaning-the-registry","title":"Cleaning the Registry","text":"<p>When leaving the game scene, clean the registry:</p> <pre><code>StellarModdingAPI.Plugin.Clean();\n</code></pre> <p>This removes the generated parts from the current game session.</p>"},{"location":"parts/creating-a-part/#complete-example","title":"Complete Example","text":"<p>A complete part mod looks like this:</p> <pre><code>using MelonLoader;\nusing Ship.Interface.Model;\nusing StellarModdingAPI.Assets;\nusing StellarModdingAPI.Items;\nusing StellarModdingAPI.Parts;\nusing UnityEngine;\n\nnamespace SDAPITest\n{\n    public static class BuildInfo\n    {\n        public const string Name = \"Example Parts Mod\";\n        public const string Description = \"Stellar Modding API part test mod\";\n        public const string Author = \"Jollyname\";\n        public const string Company = null;\n        public const string Version = \"1.0.0\";\n        public const string DownloadLink = null;\n    }\n\n    public class SDAPITest : MelonMod\n    {\n        public static AssetLoader Loader;\n\n        public override void OnLateInitializeMelon()\n        {\n            Loader = new AssetLoader(\n                MelonAssembly.Assembly,\n                LoggerInstance,\n                new[]\n                {\n                    \"PottedPlant\", \"PottedPlantThumbnail\"\n                }\n            );\n        }\n\n        public override void OnSceneWasLoaded(int buildIndex, string sceneName)\n        {\n            if (buildIndex != 1) return;\n\n            StellarModdingAPI.Plugin.Initialize();\n\n            PartDefinition PottedPlantDefinition = new()\n            {\n                Name = \"Potted Plant\",\n                Description = \"Potted plant!!!!!!!\",\n\n                Prefab = Loader.GetAsset&lt;GameObject&gt;(\"PottedPlant\"),\n                Thumbnail = Loader.GetAsset&lt;Texture2D&gt;(\"PottedPlantThumbnail\"),\n\n                Size = Vector3.one,\n                Mass = 1f,\n\n                Snapping = SnappingStyle.PreciseOnAny,\n                BuildingCost =\n                [\n                    ItemCost.Of(100, 2)\n                ]\n            };\n\n            StellarModdingAPI.Plugin.RegisterPart(PottedPlantDefinition);\n\n            StellarModdingAPI.Plugin.Build();\n        }\n\n        public override void OnSceneWasUnloaded(int buildIndex, string sceneName)\n        {\n            if (buildIndex != 1) return;\n\n            StellarModdingAPI.Plugin.Clean();\n        }\n    }\n}\n</code></pre> <p>Congratulations! You have created your first custom StellarDrive part.</p>"},{"location":"parts/item-cost-reference/","title":"ItemCost Reference","text":"<p><code>ItemCost</code> is a helper class used to create item requirements for custom parts.</p> <p>It converts an item ID and an amount into the game's internal <code>ItemInstance</code> format, which is then used by <code>PartDefinition.BuildingCost</code>.</p> <p>Namespace:</p> <pre><code>using StellarModdingAPI.Items;\n</code></pre>"},{"location":"parts/item-cost-reference/#creating-an-item-cost","title":"Creating an Item Cost","text":"<p>Item costs are created using:</p> <pre><code>ItemCost.Of(uint id, int amount)\n</code></pre> <p>Example:</p> <pre><code>ItemCost.Of(100, 5)\n</code></pre> <p>This creates a building requirement of:</p> <ul> <li>Item ID: <code>100</code></li> <li>Amount: <code>5</code></li> </ul> <p>The item ID is used to find the corresponding item registered by StellarDrive.</p> <p>For a list of available item IDs, see the Item IDs reference.</p>"},{"location":"parts/item-cost-reference/#multiple-costs","title":"Multiple Costs","text":"<p>A part can require multiple items by adding multiple <code>ItemCost</code> entries.</p> <p>Example:</p> <pre><code>BuildingCost =\n[\n    ItemCost.Of(100, 5),\n    ItemCost.Of(101, 2)\n]\n</code></pre> <p>This creates a part requiring:</p> <ul> <li>5 of item ID <code>100</code></li> <li>2 of item ID <code>101</code></li> </ul>"},{"location":"parts/item-cost-reference/#complete-example","title":"Complete Example","text":"<pre><code>PartDefinition PottedPlantDefinition = new()\n{\n    Name = \"Potted Plant\",\n    Description = \"A decorative plant\",\n\n    Prefab = Loader.GetAsset&lt;GameObject&gt;(\"PottedPlant\"),\n    Thumbnail = Loader.GetAsset&lt;Texture2D&gt;(\"PottedPlantThumbnail\"),\n\n    Size = Vector3.one,\n    Mass = 1f,\n\n    Snapping = SnappingStyle.PreciseOnAny,\n\n    BuildingCost =\n    [\n        ItemCost.Of(100, 2),\n    ]\n};\n</code></pre>"},{"location":"parts/item-ids-reference/","title":"Items","text":"<p>This page contains the item IDs available in StellarDrive.</p> <p>Item IDs are used when creating building costs for custom parts with <code>ItemCost</code>.</p> <p>Example:</p> <pre><code>BuildingCost =\n[\n    ItemCost.Of(100, 5)\n]\n</code></pre> <p>The first value is the item ID, and the second value is the required amount.</p>"},{"location":"parts/item-ids-reference/#available-items","title":"Available Items","text":"ID Name 0 Build Tool 1 Recycler Tool 2 Mining Laser 100 Fuel 101 Iron 102 Glass 103 Exotic Matter 104 Aluminum"},{"location":"parts/item-ids-reference/#finding-item-ids","title":"Finding Item IDs","text":"<p>The StellarModdingAPI can display all available items and their IDs while the game is running.</p> <p>Press: <code>F8</code></p> <p>The item list will be printed to the MelonLoader console.</p> <p>Use these IDs with ItemCost.Of() when creating custom part building costs.</p>"},{"location":"parts/part-definition-reference/","title":"PartDefinition Reference","text":"<p><code>PartDefinition</code> contains all information required by the StellarModdingAPI to create a custom StellarDrive part.</p> <p>Namespace:</p> <pre><code>using StellarModdingAPI.Parts;\n</code></pre> <p>Example:</p> <pre><code>PartDefinition definition = new()\n{\n    Name = \"Potted Plant\",\n    Description = \"A decorative plant\",\n\n    Prefab = Loader.GetAsset&lt;GameObject&gt;(\"PottedPlant\"),\n    Thumbnail = Loader.GetAsset&lt;Texture2D&gt;(\"PottedPlantThumbnail\"),\n\n    Size = Vector3.one,\n    Mass = 1f,\n\n    Snapping = SnappingStyle.PreciseOnAny\n};\n</code></pre>"},{"location":"parts/part-definition-reference/#properties","title":"Properties","text":""},{"location":"parts/part-definition-reference/#name","title":"Name","text":"<pre><code>string Name\n</code></pre> <p>The display name of the part shown in the StellarDrive build menu.</p> <p>Example:</p> <pre><code>Name = \"Potted Plant\"\n</code></pre>"},{"location":"parts/part-definition-reference/#description","title":"Description","text":"<pre><code>string Description\n</code></pre> <p>The description displayed when viewing the part in the build menu.</p> <p>Example:</p> <pre><code>Description = \"A decorative plant\"\n</code></pre>"},{"location":"parts/part-definition-reference/#prefab","title":"Prefab","text":"<pre><code>GameObject Prefab\n</code></pre> <p>The Unity <code>GameObject</code> used as visuals when the part is placed in the world, it should only contain a mesh renderer with materials and a mesh filter.</p> <p>It can be loaded from an AssetBundle using <code>AssetLoader</code>.</p> <p>Example:</p> <pre><code>Prefab = Loader.GetAsset&lt;GameObject&gt;(\"PottedPlant\")\n</code></pre>"},{"location":"parts/part-definition-reference/#thumbnail","title":"Thumbnail","text":"<pre><code>Texture2D Thumbnail\n</code></pre> <p>The image displayed for the part in the build menu.</p> <p>Example:</p> <pre><code>Thumbnail = Loader.GetAsset&lt;Texture2D&gt;(\"PottedPlantThumbnail\")\n</code></pre>"},{"location":"parts/part-definition-reference/#size","title":"Size","text":"<pre><code>Vector3 Size\n</code></pre> <p>Controls the occupied building space of the part in the building grid.</p> <p>Example:</p> <pre><code>Size = Vector3.one\n</code></pre> <p><code>Vector3.one</code> creates a part occupying a <code>1x1x1</code> building space.</p>"},{"location":"parts/part-definition-reference/#mass","title":"Mass","text":"<pre><code>float Mass\n</code></pre> <p>Defines the physical mass of the part.</p> <p>Example:</p> <pre><code>Mass = 1f\n</code></pre>"},{"location":"parts/part-definition-reference/#snapping","title":"Snapping","text":"<pre><code>SnappingStyle Snapping\n</code></pre> <p>Controls how the part snaps to other building pieces.</p> <p>Example:</p> <pre><code>Snapping = SnappingStyle.PreciseOnAny\n</code></pre>"},{"location":"parts/part-definition-reference/#buildingcost","title":"BuildingCost","text":"<pre><code>ItemCost[] BuildingCost\n</code></pre> <p>Defines the resources required to build the part.</p> <p>Example:</p> <pre><code>BuildingCost =\n[\n    ItemCost.Of(100, 2)\n]\n</code></pre> <p>See the ItemCost Reference for more information.</p>"}]}